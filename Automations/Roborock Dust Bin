alias: Roborock Dust Bin
description: Empty the trail every 2 cleanings
triggers:
  - value_template: "{{ states('sensor.roborock_s7_anzahl_reinigungen') | int % 2 == 0 }}"
    trigger: template
conditions: []
actions:
  - action: notify.mobile_app_sm_s901b
    metadata: {}
    data:
      message: Dust bin is full!
mode: single
